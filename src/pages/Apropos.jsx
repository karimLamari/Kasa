import React from 'react';
import Header from '../components/Header/Header';

const Apropos = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default Apropos;